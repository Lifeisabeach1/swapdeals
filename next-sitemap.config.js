/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://swapdeals.se', // Replace with your actual domain
  generateRobotsTxt: true, // (optional) Generate robots.txt
  sitemapSize: 7000,        // optional, max entries per sitemap file
  changefreq: 'daily',
  priority: 0.7,
};
